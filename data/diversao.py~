# ideia: calcular a media de money dado aos projetos da mesma àrea
# mostrar um diagrama de barras com 

d_area = {}
area = []

a = f.readline()
area.append(a[3]) # ler a coluna 4
d[area[-1]] = []
sum_pedida = 0
sum_dada = 0
numb = 0
while a[3] == area[-1]:
	numb = numb +1
	sum_pedida = a[10]
	sum_dada = a[11]
d_area[area[-1]] = [(sum_pedida*1.)/numb,(sum_dada*1.)/numb]

from pylab import *
plot(d_area.keys(),[d_area[i][0] for i in area],'-o')
plot(d_area.keys(),[d_area[i][1] for i in area],'-o')
ylabel('Area')
xlabel('Pasta')
title('Pasta e areas cientificas')
show()
